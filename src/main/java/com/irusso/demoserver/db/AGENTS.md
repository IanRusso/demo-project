Each database table will have its own class in the dao package to access the data e.g. UserDao will allow you to query the user table

For data models that representing concepts exclusively related to the database, they will be stored in the model package e.g. SqlResponse