Each REST resource has its own class in the server package e.g. /user will have a class called UserResource

The corresponding data models are stored in the model package e.g. there would be a bean class called User